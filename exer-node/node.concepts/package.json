{
	"name": "loggar_nodejs_01_basic",
	"version": "0.1.0",
	"scripts": {
		"test": "echo \"Error: no test specified! Configure in package.json\" && exit 1"
	},
	"dependencies": {
		"connect": "^3.6.6",
		"mongoose": "^5.2.6"
	}
}